
harp{
  gui{
    location = 'tools'
      .type = str
      .help = The tab item where the HARp GUI shall appear.
    before = 'images'
      .type = str
      .help = The tool before which the HARp GUI shall appear.
    }
  check_output_interval = 0
    .type = int
  processing_gif = giphy.gif
    .type = str
    
}
